SELECT *
FROM Employees
WHERE salary IS NULL;

SELECT *
FROM Orders
WHERE discount IS NOT NULL;

SELECT product_name,
     category
    FROM Products
  WHERE category IS NULL;
  
 SELECT COUNT(*)
 FROM Employees
 WHERE manager_id IS NULL;
 
 SELECT IFNULL(salary,0) AS salary_amount
 FROM Employees;
 
 SELECT IFNULL(bonus,0) AS bouns_amount
 FROM Employees;
 
 SELECT 
 IFNULL(amount,500)
 FROM Orders;
 
 SELECT IFNULL(stock,0)
 FROM Products;
 
 SELECT COALESCE(salary, bonus) AS earnings
FROM Employees;


SELECT product_name,
       COALESCE(price, 1000) AS product_price
FROM Products;

 SELECT customer_name,
       COALESCE(amount, discount, 0) AS payment
FROM Orders;

SELECT  NULLIF(salary, 0) AS salary
FROM Employees;

SELECT  NULLIF(discount, 0) AS discount
FROM Orders;

SELECT COALESCE(salary,0)+COALESCE(bonus,0) AS total_earnings
FROM Employees;

SELECT *
FROM Employees
WHERE salary IS NULL
  AND bonus IS NULL;

SELECT *
FROM Products
WHERE price IS NULL
AND category IS NOT NULL;

SELECT COALESCE(salary,bonus,1000)AS income
FROM Employees;




